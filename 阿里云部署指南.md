# TOOP & PP'S ATLAS 阿里云部署指南

这份文档用于把网站部署到阿里云 ECS，并连接阿里云 RDS PostgreSQL 和 OSS 对象存储。

## 一、部署前检查

开始部署前，请确认已经创建以下资源：

- 一台阿里云 ECS 服务器，建议使用 Ubuntu 22.04 或更新版本。
- 一个阿里云 RDS PostgreSQL 实例。
- 一个私有的阿里云 OSS Bucket。
- 一个可以解析到 ECS 公网 IP 的域名。
- 一个用于存放源代码的私人 GitHub、Gitee 或阿里云 Codeup 仓库。

> 安全提醒：之前在聊天中发送过的数据库密码已经暴露，请先在 RDS 控制台重置密码。不要把新密码发送到聊天、邮件或截图中，也不要提交到 Git 仓库。

## 二、检查 ECS 和 RDS 网络

ECS 和 RDS 应位于香港地域的同一个 VPC 中，这样网站可以通过内网连接数据库。

1. 打开阿里云 ECS 控制台，记录 ECS 的私网 IP。
2. 打开 RDS 控制台，进入目标 PostgreSQL 实例。
3. 找到“白名单与安全组”设置。
4. 把 ECS 私网 IP 加入 RDS 白名单。

例如 ECS 私网 IP 是 `172.16.0.12`，白名单可以填写：

```text
172.16.0.12/32
```

应用程序应使用 RDS 的内网连接地址，不要为网站申请或使用数据库公网地址。

## 三、配置 OSS 权限

OSS Bucket 应保持私有，照片由网站服务器读取并返回给浏览器。

推荐给 ECS 绑定 RAM 实例角色，例如 `atlas-ecs-role`，并只授予目标 Bucket 所需的权限：

- `oss:ListObjects`
- `oss:GetObject`
- `oss:PutObject`
- `oss:DeleteObject`

网站会通过 ECS 元数据服务获取临时凭据，因此 ECS 上不需要保存长期 AccessKey。

如果尚未创建 RAM 实例角色，请按照以下顺序操作：

1. 打开阿里云 RAM 控制台。
2. 创建可信实体类型为“阿里云服务”的 RAM 角色。
3. 受信服务选择“云服务器 ECS”。
4. 给角色添加仅允许访问目标 OSS Bucket 的权限策略。
5. 回到 ECS 控制台，把该 RAM 角色绑定到运行网站的 ECS 实例。

## 四、上传代码到私人 Git 仓库

迁移后的代码位于 `codex/aliyun-migration` 分支。

先在 GitHub、Gitee 或阿里云 Codeup 创建一个私人仓库，然后在本机 PowerShell 中执行：

```powershell
cd C:\Users\cresc\Documents\Codex\2026-08-30\new-chat
git checkout codex/aliyun-migration
git remote add aliyun 你的私人仓库地址
git push -u aliyun codex/aliyun-migration
```

如果已经存在名为 `aliyun` 的远程仓库，可以先查看：

```powershell
git remote -v
```

需要修改地址时执行：

```powershell
git remote set-url aliyun 你的私人仓库地址
git push -u aliyun codex/aliyun-migration
```

## 五、连接 ECS

在本机终端中执行：

```bash
ssh root@你的ECS公网IP
```

如果使用 SSH 私钥，则执行：

```bash
ssh -i /你的私钥路径/服务器密钥.pem root@你的ECS公网IP
```

## 六、安装 Docker、Git 和 Nginx

下面的命令适用于 Ubuntu：

```bash
sudo apt update
sudo apt install -y git nginx curl ca-certificates
curl -fsSL https://get.docker.com | sudo sh
sudo systemctl enable --now docker nginx
sudo usermod -aG docker "$USER"
```

执行完成后退出 SSH 并重新连接，让 Docker 用户组权限生效：

```bash
exit
ssh root@你的ECS公网IP
```

重新登录后验证 Docker：

```bash
docker --version
docker compose version
```

## 七、在 ECS 下载代码

```bash
sudo mkdir -p /opt/make-love-atlas
sudo chown -R "$USER:$USER" /opt/make-love-atlas
git clone --branch codex/aliyun-migration 你的私人仓库地址 /opt/make-love-atlas
cd /opt/make-love-atlas
```

如果私人仓库需要身份验证，建议给 ECS 配置只读 SSH Deploy Key，不要把账户密码直接写在仓库地址里。

## 八、创建生产环境配置

复制项目提供的环境变量模板：

```bash
cd /opt/make-love-atlas
cp .env.example .env
nano .env
```

填写以下内容：

```dotenv
NODE_ENV=production
APP_ORIGIN=https://你的域名

DB_HOST=你的PostgreSQL内网连接地址
DB_PORT=5432
DB_NAME=atlas
DB_USER=atlas_app
DB_PASSWORD=你刚刚重置的新密码
DB_SSL=false

OSS_REGION=oss-cn-hongkong
OSS_BUCKET=你的OSS-Bucket名称
ALIBABA_CLOUD_ECS_ROLE_NAME=atlas-ecs-role
```

字段说明如下：

- `APP_ORIGIN` 是网站最终使用的 HTTPS 地址。
- `DB_HOST` 只填写 RDS 内网域名，不要加 `postgresql://`，也不要加端口或数据库名称。
- `DB_PORT` 是 PostgreSQL 默认端口 `5432`。
- `DB_NAME` 是数据库名称。
- `DB_USER` 是数据库账号。
- `DB_PASSWORD` 是重置后的新密码。
- `DB_SSL` 在同 VPC 内网连接时通常设置为 `false`；如果 RDS 强制 SSL，则改为 `true`。
- `OSS_REGION` 在香港地域通常是 `oss-cn-hongkong`。
- `OSS_BUCKET` 只填写 Bucket 名称，不要填写完整网址。
- `ALIBABA_CLOUD_ECS_ROLE_NAME` 是绑定到 ECS 的 RAM 角色名称。

保存文件后限制读取权限：

```bash
chmod 600 .env
```

不要执行 `git add .env`，也不要把 `.env` 内容发给任何人。

## 九、构建并启动网站

在项目目录执行：

```bash
cd /opt/make-love-atlas
docker compose build
docker compose up -d
docker compose ps
docker compose logs --tail=150
```

第一次构建需要下载 Node.js 镜像和项目依赖，耗时可能较长。

网站只绑定到 ECS 本机的 `127.0.0.1:3000`，外部访问会经过 Nginx。

验证网站是否已经启动：

```bash
curl -I http://127.0.0.1:3000/
```

验证数据库连接：

```bash
docker compose exec atlas node scripts/check-db.mjs
```

如果数据库连接成功，程序会自动创建 `footprints`、`photos` 和相关数据库表，并初始化预设城市。

## 十、配置 Nginx

复制项目提供的 Nginx 配置：

```bash
sudo cp deploy/nginx.conf /etc/nginx/sites-available/atlas
sudo nano /etc/nginx/sites-available/atlas
```

把文件中的 `atlas.example.com` 全部替换成你的真实域名，然后执行：

```bash
sudo ln -s /etc/nginx/sites-available/atlas /etc/nginx/sites-enabled/atlas
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

如果软链接已经存在，不需要重复创建，可以直接测试并重载 Nginx。

Nginx 已配置较大的上传限制，用于支持照片上传。

## 十一、配置 ECS 安全组

在阿里云 ECS 控制台中设置安全组入方向规则：

| 端口 | 来源 | 用途 |
| --- | --- | --- |
| `80` | `0.0.0.0/0` | HTTP 访问和证书验证 |
| `443` | `0.0.0.0/0` | HTTPS 网站访问 |
| `22` | 你自己的公网 IP | SSH 管理服务器 |

不要向公网开放 `3000` 和 `5432` 端口。

## 十二、配置域名解析

在域名 DNS 控制台添加 A 记录：

```text
记录类型：A
主机记录：atlas 或 @
记录值：ECS 公网 IP
```

例如使用 `atlas.example.com`，主机记录填写 `atlas`。

等待解析生效后，可以执行以下命令检查：

```bash
ping 你的域名
```

返回的 IP 应与 ECS 公网 IP 一致。

## 十三、配置 HTTPS

域名解析生效后，在 ECS 上执行：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名
```

验证证书自动续期：

```bash
sudo certbot renew --dry-run
```

确认 `.env` 中的 `APP_ORIGIN` 是最终 HTTPS 地址，然后重启网站：

```bash
cd /opt/make-love-atlas
docker compose up -d --force-recreate
```

## 十四、上线验收

部署完成后，分别从深圳和新加坡测试以下功能：

- 网站可以正常打开。
- 背景图片和幻灯片可以正常播放。
- 地球仪可以旋转、放大和缩小。
- 所有预设城市都可以选择。
- 可以输入城市名称并添加新城市。
- 可以多次向同一个城市添加照片。
- 可以删除照片。
- 可以删除临时添加的城市。
- 刷新页面后，城市和照片仍然存在。
- Safari、Chrome 和手机浏览器操作流畅。

在这些检查全部通过前，建议保留现有 `chatgpt.site` 网站作为临时备用版本。

## 十五、以后更新网站

代码更新并推送到私人仓库后，在 ECS 上执行：

```bash
cd /opt/make-love-atlas
git pull
docker compose build
docker compose up -d
docker compose logs --tail=100
docker image prune -f
```

`docker image prune -f` 只会清理未被使用的旧镜像，用于释放磁盘空间。

## 十六、常用排错命令

查看容器状态：

```bash
docker compose ps
```

查看网站日志：

```bash
docker compose logs --tail=200 atlas
```

持续查看网站日志：

```bash
docker compose logs -f atlas
```

检查 Nginx 配置：

```bash
sudo nginx -t
```

查看 Nginx 错误日志：

```bash
sudo tail -n 200 /var/log/nginx/error.log
```

检查数据库：

```bash
docker compose exec atlas node scripts/check-db.mjs
```

重启网站：

```bash
docker compose restart atlas
```

如果部署失败，可以发送不包含密码、AccessKey、Cookie 和私钥的错误日志进行排查。
